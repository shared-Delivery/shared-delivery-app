var assert = require('assert');

describe('Array', () => {
  it('testing ', () => {
    assert.equal('hola', 'hola');
  });
});

describe('Array', () => {
  it('testing ', () => {
    assert.equal('hola', 'hola');
  });
});

describe('Array', () => {
  it('testing ', () => {
    assert.equal('andres', 'andres');
  });
});
